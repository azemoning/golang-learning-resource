package main

import "fmt"

func main() {

	// function main must be within the same package main
	// so if we want to create func main()
	// then we need to place it under the main package

	fmt.Println("Hello, World!")
}
